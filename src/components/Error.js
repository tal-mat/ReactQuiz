// Displays an error message indicating a problem with fetching questions.

function Error() {
  return (
    <p className="error">
      <span>💥</span> There was an error fetching questions.
    </p>
  );
}

export default Error;
